# 에코타운 v5.2: 층 분리 및 선큰 검토 모델

## 배포

이 폴더의 `index.html`만 GitHub Pages 저장소의 기존 파일과 교체합니다. 사진, GeoJSON, 스타일 코드가 모두 내장되어 있습니다. 기존 v5.1은 별도 폴더에 보존했습니다.

지도 설정에서 **에코타운 선큰 구조 보기**를 누르면 구조를 확대합니다. **사진의 층 구분색 보기**를 켜면 지하 노출부는 초록, 지상 1층은 빨강, 상부층은 노랑으로 바뀝니다. **파란 검토 동선 보기**로 새 선을 켜거나 끌 수 있습니다. 기존 KML 참고 동선과 이번 사진 해석 동선은 별도 레이어입니다.

## 기준 높이

Mapbox `fill-extrusion-base`의 최소값은 0입니다. 음수를 직접 전달하지 않고 지하 바닥을 0으로 옮깁니다. 지상 기준 높이를 signed relative 값으로 별도 보존합니다.

| 구조 | 지상 기준 상대 높이 | 렌더링 base → top |
|---|---|---|
| 지하 1층 | -4 → 0m | 0 → 4m |
| 지상 1층 | 0 → 4m | 4 → 8m |
| 지상 2층 | 4 → 8m | 8 → 12m |
| 지상 3층: 메인동 | 8 → 12m | 12 → 16m |

계산식: `렌더링 높이 = 지상 기준 상대 높이 + 4m`.

층수와 색상 의미는 사용자 지시를 반영했습니다. 4m 층고·지하 깊이는 실측값이 아닌 모델링 가정입니다. 이 기준은 에코타운 구조를 설명하는 로컬 시각 기준이며 해발고도나 지형 DEM이 아닙니다. 기존 지도 전체 지형을 실제 높이로 재구축한 것은 아닙니다.

## 구조 변경

- 일률적인 v5.1 받침대·일자 단차 보행로를 제거했습니다.
- 메인동은 B1 노출부, G1, G2, G3, 지붕, 태양광 패널을 별도 Feature로 구성했습니다.
- 나머지 3개 동은 G1/G2와 지붕을 분리했습니다. 사진에서 초록 표시가 확인된 메인·우측·하단의 안쪽 외벽을 지하 노출부로 구분했습니다. 좌측동에 확인되지 않은 지하 유리 외벽을 추가하지 않았습니다.
- 매립부는 흙색 구조체, 외곽 경사면 대응부는 지면 높이까지의 짧은 토사 도형으로 처리했습니다. 노출 외벽과 속성이 다릅니다. 연속 경사면 메시·정밀 토공 단면 모델은 아닙니다.
- 중앙 삼각형 광장은 내부 링(hole)이 있는 지형 Polygon으로 만들었습니다. 구멍 안 바닥을 지하층 높이에 두고 옹벽을 별도로 구성했습니다. 지하 구역을 높은 평판으로 덮지 않았습니다.
- 하단 선큰 정원과 지상 연결 보행면을 각각 다른 높이에 놓았습니다. 파란 선은 해당 보행면 위 높이로 올렸습니다.

## 보행 네트워크

26개 노드와 25개 링크입니다. 기존 KML 에코타운 정문 점을 하나의 기준점으로 사용하고 나머지는 사진의 상대 배치와 기존 OSM 외곽을 대조하여 구성했습니다. 항공사진은 비스듬한 사진이며 지상 기준점이 있는 정사영상이 아닙니다. 정확한 픽셀-위경도 변환 결과가 아닙니다.

- `from_node`, `to_node`: 공유 접속점
- `from_level`, `to_level`: B1 / G1 / transition
- `relative_start_m`, `relative_end_m`: 상대 높이
- `render_z_m`: 파란 선 표시 높이
- `vertical_transition`: 층간 이동인지 여부
- `routing_enabled: false`, `survey_verified: false`, `access: unknown`

지하·지상 사이 연결은 16개의 짧은 구간으로 분할해 단차를 표현했습니다. 실제 계단 수를 뜻하지 않으며 계단·경사로·접근성은 현장 확인 전 가정입니다. 새 출입구 연결점도 실제 출입구로 확정하지 않았습니다. 서로 다른 높이의 선을 평면 교차만으로 연결하지 않았습니다.

**기존 GPS 보정·길찾기에는 이 네트워크를 넣지 않았습니다.** 기존 KML/OSM 데이터와 GPS 함수는 그대로 보존했습니다. 새로운 선들은 원본을 덮어쓰지 않는 검토용입니다. 새 동선을 실제 안내에 사용하려면 출입구 위치·층간 연결·통행 가능 여부를 현장에서 확인해야 합니다.

## 파일과 코드

- `ecotown-structures.geojson`: 에코타운 구조 Feature 89개
- `ecotown-walkways.geojson`: 검토 동선 LineString 25개
- `review-network.geojson`: 노드와 링크를 함께 포함
- `visual-models.geojson`: 기존 박물관·정문 모델까지 포함한 전체 시각 도형
- `mapbox-layers.js`: 독립 모듈에서 사용할 레이어 추가 함수. 지도 load 이후 호출하고 두 GeoJSON 객체를 전달합니다. 내장 HTML에서는 이미 적용되어 있어 중복 호출하지 않습니다.
- `build.cjs`: 기존 v5.1 HTML을 보존하면서 v5.2를 생성
- `model-config.json`: 높이 기준과 건물 역할 기록

구조 속성 예:

```json
{
  "band": "B1",
  "relative_base_m": -4,
  "relative_top_m": 0,
  "base_m": 0,
  "top_m": 4,
  "base": 0,
  "height": 4,
  "survey_verified": false,
  "routing_enabled": false
}
```

`base`와 `height`는 기존 모델과의 호환을 위해 `base_m`, `top_m`와 같은 값으로 제공합니다. 구조 레이어는 다음처럼 읽습니다.

```js
paint: {
  'fill-extrusion-base': ['get', 'base'],
  'fill-extrusion-height': ['get', 'height'],
  'fill-extrusion-color': ['get', 'color']
}
```

선 레이어에는 `line-elevation-reference: 'ground'`와 `line-z-offset: ['get', 'render_z_m']`를 적용했습니다. 현재 앱처럼 별도 terrain DEM을 사용하지 않는 조건의 로컬 기준입니다. terrain을 추가할 경우 높이 기준을 다시 맞춰야 합니다.

공식 사양: https://docs.mapbox.com/style-spec/reference/layers/#fill-extrusion-base , https://docs.mapbox.com/style-spec/reference/layers/#line-z-offset

## 검증 범위

구문 검사, 기존 데이터·토큰·사진·GPS 함수 동일성, 높이 범위, 링 폐합·자기교차, 선큰 구멍 포함 관계, 연결점 공유, 네트워크 연결성, 통행선의 건물 관통 여부를 검사했습니다. 모의 DOM/Mapbox 환경에서 초기화와 구분색·동선 표시 전환도 통과했습니다.

실제 위치·높이 정확도 및 새 배포본의 Mapbox 렌더링은 이번 검증에 포함되지 않습니다. 이전 버전에서 사용자가 확인한 GPS·스마트폰 성능을 새 지형 모델의 실측 검증으로 간주하지 않았습니다. `geometry-plan.png`는 생성된 도형의 평면 검사도이며 Mapbox 캡처가 아닙니다.
